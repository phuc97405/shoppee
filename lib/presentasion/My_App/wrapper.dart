import 'package:flutter/material.dart';

import 'package:shoppee/presentasion/Auth/splash.dart';

class WrapperScreen extends StatelessWidget {
  // final WrapperController c = Get.put(WrapperController());

  @override
  Widget build(BuildContext context) {
    return SplashScreen();
  }
}
